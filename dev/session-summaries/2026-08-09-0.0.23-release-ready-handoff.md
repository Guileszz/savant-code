# 0.0.23 Release-Ready — End-of-Session Handoff

## Scope

This session completed the release-readiness audit for the pending `0.0.23` target and closed
out the FID ledger. The full release gate suite was run end-to-end and made green, the v0.0.23
release notes were drafted, and all program FIDs were closed and archived with Nova sign-offs.
Per operator instruction, **no commit, tag, push, publication, or deployment was performed** —
the tree is staged for a morning release run.

## Governance evidence

- All three independent Nova audits are **PASS** and recorded in `dev/nova/inbox/`:
  - FIDs 003–010 optimization/automation implementation sign-off response
  - FID-011 graph-export decomposition audit response
  - FIDs 012–018 optimization-program implementation audit response
- FIDs 003–010, 011, and 012–018 are all closed and archived under `dev/fids/archive/`.
- `dev/fids/` holds **zero active FIDs** (only `README.md` + `archive/`).
- Active documents follow the no-signature policy; historical records were preserved.

## Completed program (this session)

- **Full release gate suite re-run and fixed to green:**
  - Typecheck × 10 workspaces: exit 0.
  - Test suites: sdk 534 (0 fail) · cli 2,938 · common 557 · agents 44 · agent-runtime 761 ·
    evals 69 · code-map 51 · database 15 · knowledge-graph 18 · llm-providers 58 · scripts 75.
  - ESLint `--max-warnings 0`, markdownlint, Prettier (full repo), `validate:repository`
    (PASS), provider-docs check: all green.
- **Gate hardening fixes:**
  - `sdk/test/setup-env.ts` auto-resolves the vendored ripgrep binary (`dist/vendor`) —
    SDK tests pass without `SAVANT_CODE_RG_PATH`.
  - Manual E2E harnesses renamed off the `*.test.ts` glob: `agents/browser-use/manual-e2e.ts`,
    `agents/librarian/manual-e2e.ts` (`git mv`; docstring usage + `docs/savant-code-modes.md`
    reference updated; eslint tooling override added). Root `bunfig.toml` unchanged.
  - Removed leftover `packages/agent-runtime/measure-tokens.tmp.ts`; prettier-formatted 16
    unformatted decomposition files from FIDs 015/016.
- **Docs:** v0.0.23 release notes drafted at `docs/release-notes-v0.0.23.md`; CHANGELOG
  012–018 entry refreshed to the closed/archived state with final gate numbers; LEARNINGS.md
  entry #8 (manual harnesses must not use the `.test.ts` suffix).

## Version policy

VERSION + all 15 workspace/release manifests are `0.0.23`. README, README.zh-CN,
`docs/SAVANT-VERSIONING.md`, `ARCHITECTURE.md`, and `CHANGELOG.md` all identify `0.0.23` as
pending/unreleased.

## Release readiness

Everything code-side is green. The only remaining items are operator actions:

- **Clean shell for the `ci` build** (FID-2026-0805-002 env-integrity gate): `.env.local` is
  present and 10 dev `NEXT_PUBLIC_*` overrides are in the shell. **Corrected 2026-08-10:**
  setting `.env.local` aside and clearing the overrides is necessary but NOT sufficient — a
  bare empty shell fails `bun run ci` with zod `Invalid environment configuration` because
  `common/src/env-schema.ts` requires the 8 core `NEXT_PUBLIC_*` vars and `common/src/env.ts`
  throws on `undefined`. The proven recipe: (1) set `.env.local` aside,
  (2) unset the dev `NEXT_PUBLIC_*` overrides, (3) inject the canonical prod block verbatim
  (`CANONICAL_NEXT_PUBLIC_DEFAULTS`, `cli/scripts/build-binary.ts:52-61` — exactly what
  `scripts/public-release.ts` `PROFILE_ENV` and CI inject), then (4) run `bun run ci`
  (SDK + Savant-Free binaries) and (5) `bun run release:public:preview`; restore `.env.local`
  afterwards. Verified GREEN 2026-08-10 (see session summary
  `2026-08-10-1144-single-agent-bootup-housekeeping.md`).
- **Commit + push** the working tree (251 changed entries: 111 modified, 124 untracked,
  14 deletions that are intentional FID-017 test-split originals, 2 renames).
- **Publish** once the release pipeline passes under the pinned Bun `1.3.14`.

## Morning checklist

1. Set aside `.env.local`; unset dev `NEXT_PUBLIC_*` overrides (`env -u` or a clean shell).
2. Inject the canonical prod env block (`CANONICAL_NEXT_PUBLIC_DEFAULTS` from
   `cli/scripts/build-binary.ts:52-61`) — required: the zod env schema throws on a bare empty
   shell (corrected 2026-08-10).
3. `bun run ci` — confirm SDK + Savant-Free binaries build clean (GREEN 2026-08-10 with the
   canonical env injected). Restore `.env.local` afterward.
4. `bun run validate:repository` and `bun run generate:provider-docs:check` (both green already).
5. Stage and commit the working tree (see release notes doc + CHANGELOG for the summary).
6. `bun run release:public:preview` to dry-run; then run the release pipeline to tag and
   publish `0.0.23`.
7. Post the GitHub release body from `docs/release-notes-v0.0.23.md`.
