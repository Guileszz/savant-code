# FID: v0.0.21 Post-Audit Fix Batch

> ESLint gate + basher phase waste + export org/perf/layout + FSM `self_correct`
> deadlock + ADHD/caveman retrofit

**Filename:** `FID-2026-0806-016-v0.0.21-post-audit-fix-batch.md`
**ID:** FID-2026-0806-016
**Severity:** high
**Status:** created
**Created:** 2026-08-06 19:40
**Author:** Savant Orchestrator (Savant session)
**YAGNI-Compliance:** Pending

---

## Summary

The v0.0.21 A–Z audit (`dev/scratchpad/az-test-v0.0.21-all-fixes-results.md`)
returned **GO WITH CAVEATS** with exactly one gate failure — an ESLint
`import/order` violation in `sdk/examples/readme-example-2.ts` — plus the audit run
itself hit a real harness deadlock in the `self_correct` FSM phase. Two Nova inbox
items are queued on top: a basher phase-enforcement waste bug report and an export
organization/performance/layout feature request, plus an open feature question on an
ADHD-friendly output mode. This FID batches all five items into one converged fix set.

## Environment

- **OS:** Windows 11 (win32, bash shell)
- **Language/Runtime:** TypeScript strict monorepo, Bun ≥ 1.3.11 (pinned 1.3.14)
- **Tool Versions:** ESLint (zero-tolerance `--max-warnings 0`), Prettier, markdownlint, OpenTUI + React CLI
- **Commit/State:** `main` ahead of `origin/main` by 1 commit; v0.0.21 unshipped (working tree)

## Detailed Description

### Finding 1 — ESLint gate failure (T1.2/T5.2)

#### Problem

`bun x eslint . --max-warnings 0` exits 1 with 2 `import/order` warnings. This is the
**only** failure in the A–Z audit, flipping the verdict from GO to GO WITH CAVEATS and
blocking the pre-push gate.

#### Expected Behavior

Zero ESLint warnings — the repo's hard gate (`.githooks/pre-push`,
`protocol.config.yaml lint`).

#### Root Cause

In `sdk/examples/readme-example-2.ts`, the `/* eslint-disable no-console */` comment on
**line 5** sits between two `@savant-code/sdk` imports (line 3 value import, line 6 type
import), splitting the import group. `import/order` flags the blank lines (lines 4 and
5) *within* the same-package group.

```text
1  import { z } from 'zod/v4'
2
3  import { SavantCodeClient, getCustomToolDefinition } from '@savant-code/sdk'
4
5  /* eslint-disable no-console -- example script: intentional console output */
6  import type { AgentDefinition } from '@savant-code/sdk'
```

#### Evidence

- `dev/scratchpad/az-test-v0.0.21-all-fixes-results.md` T1.2/T5.2 FAIL (2 warnings, exit 1)
- `sdk/examples/readme-example-2.ts:3-6` (quoted above)
- FID-2026-0806-008 claimed the fix but it was never applied

### Finding 2 — Basher subagent phase-enforcement waste (Nova bug report)

#### Problem

The basher subagent wastes ~500 tokens on a "failure ceremony" when spawned in the
`idle` phase: it tries `run_terminal_command`, fails, analyzes why, explains the
failure in detail, and offers alternatives. The failure is predictable —
`run_terminal_command` is FSM-gated.

#### Expected Behavior

Basher fails fast with a one-line report, or the parent transitions phase before spawning so the command runs first try.

#### Root Cause

`packages/agent-runtime/src/tools/tool-executor/native.ts:170` gates
`run_terminal_command` to `['audit', 'green']` only. The orchestrator system prompt
*already* instructs transitioning before spawning terminal-capable subagents
(`agents/savant/system-prompt.ts:143`), but the basher's own prompt has no fail-fast
self-check, so a spawn from `idle` burns tokens explaining the inevitable block.

#### Evidence

- `dev/nova/inbox/2026-08-06-basher-phase-enforcement-bug-report.md` (full transcript of
  the ~500-token ceremony)
- `packages/agent-runtime/src/tools/tool-executor/native.ts:163-174` —
  `!['audit', 'green'].includes(agentState.fsmPhase ?? 'idle')` → block message
- `agents/basher.ts:42` — `toolNames: ['run_terminal_command']`

### Finding 3 — Export organization + performance + layout (Nova feature request)

#### Problem

1. **Root clutter:** `/export` writes `savant-export-{ts}.html` and `/graph-export` writes
   `savant-graph-{ts}.html` to the CWD, accumulating in the project root.
2. **Load time:** 3.8–4.4 MB self-contained HTML files take 2+ minutes to load in a
   browser (inline Font Awesome + Cytoscape + full `GRAPH_DATA` JSON).
3. **Sidebar:** the graph sidebar participates in page flex layout instead of being
   viewport-fixed, forcing page scroll.
4. **Layout:** 6,916 nodes bunch/overlap on cold open (COSE params insufficient at that
   scale).

#### Expected Behavior

Exports land in an organized `dev/exports/` structure with single-file rotation; graph
export loads fast; sidebar stays in-viewport; nodes spread and zoom-to-fit on load.

#### Root Cause

- `cli/src/commands/export-conversation.ts:60` — `path.resolve(process.cwd(), 'savant-export-{ts}.html')`
- `cli/src/commands/graph-export.ts:66` — `path.resolve(process.cwd(), 'savant-graph-{ts}.html')`
- `cli/src/commands/graph-export/template.ts:130` — `var GRAPH_DATA = ${graphJson};`
  (eager, inline; no lazy/deferred init)
- `cli/src/commands/graph-export/template.ts:531-535` — `.graph-sidebar` is a flex child
  (`overflow-y: auto`), not `position: fixed`
- `cli/src/commands/graph-export/template.ts:209-211` — COSE params tuned for small graphs
  only; no zoom-to-fit + cluster positioning on load

#### Evidence

- `dev/nova/inbox/2026-08-06-export-organization-and-layout-fixes.md` (referencing
  `savant-graph-2026-08-06T23-10-04.html` + `savant-export-2026-08-06T23-17-44.html`)
- The 3.8 MB export the user attached to this session (`savant-export-2026-08-06T23-17-44.html`, 4,001,784 bytes)

### Finding 4 — FSM `self_correct` verification deadlock (discovered in the A–Z run)

#### Problem

During the audit run, after writing the results report the agent was in `self_correct`
and needed to run verification (`run_terminal_command`) to clear the Law 3
"unverified file(s)" gate — but the runtime blocks `run_terminal_command` in
`self_correct`. Transitions out of `self_correct` are `green|complete|idle`; `green`
is additionally FID-gated (no open FIDs in a read-only audit), so the agent deadlocked
and could only force `complete`.

#### Expected Behavior

The documented contract says `self_correct` **may verify inline** —
`common/src/constants/agents.ts:202` lists `run_terminal_command (phase-gated)` for
self_correct, and ECHO.md SELF-CORRECT row implies verification before `complete`.
The runtime must match the doc.

#### Root Cause

Runtime/contract mismatch: `native.ts:170` only allows `['audit', 'green']` for
`run_terminal_command`, but the documented phase table grants `self_correct` the tool.

#### Evidence

- `packages/agent-runtime/src/tools/tool-executor/native.ts:170` — `!['audit', 'green'].includes(...)`
- `common/src/constants/agents.ts:202` — self_correct row includes `run_terminal_command (phase-gated)`
- `packages/agent-runtime/src/tools/handlers/tool/transition-phase.ts:29-37` —
  `self_correct: ['green', 'complete', 'idle']` (no `audit`)
- `packages/agent-runtime/src/tools/handlers/tool/transition-phase.ts:83-97` — FID-presence
  gate on `green`
- `packages/agent-runtime/src/echo/pre-write-gates.ts:81-90` — Law 3 block on unverified
  writes
- Session transcript: `ECHO Enforcement BLOCKED: Law 3 … 1 unverified file(s) …` then
  `INVALID FSM transition: self_correct → audit` then `Cannot transition to GREEN: no FID
  files present`

### Finding 5 — ADHD-friendly output mode (Nova feature question) — retrofit decision

#### Problem

Nova asks whether to implement an ADHD-friendly output mode (10 rules from
`i-have-adhd`, 17.7k★) and which integration option (A: new mode / B: agent-specific /
C: skill / D: hybrid).

#### Operator guidance (2026-08-06)

> "We are simply taking the idea and perfecting it to our system, not a direct 1:1 port…
> if we don't really need it, say that, if we can retrofit it, say that, the idea is not
> concrete."

#### Honest assessment

The repo **already ships** an opt-in Caveman telegraphic-output mode (P5f,
`protocol.config.yaml caveman.enabled`, applied at the runtime prompt boundary to
Orchestrator/Detective/Scribe — `packages/agent-runtime/src/util/caveman-rules.ts`,
wired at `packages/agent-runtime/src/templates/strings.ts:290`). The 10 ADHD rules
overlap heavily with what Caveman already does: *no preamble / no closers / suppress
tangents / matter-of-fact errors / lead with action* are all Caveman staples. The
truly new, useful rules are:

1. **Number multi-step tasks** (rule 02) — Caveman is fragment style; numbering is additive
2. **Cap lists at 5 items** (rule 09) — small, cheap, works with fragments
3. **End with one concrete next step** (rule 03) — pairs with existing no-preamble style
4. **Specific time estimates** (rule 06) — genuinely new; low value for an agent (no wall-clock grounding), **drop**

Not needed as a new mode (the mode axis is execution scope —
HYBRID/SCAFFOLD/STRICT/ANALYZE — output formatting does not belong there), not needed
as a skill (Caveman config already exists and is opt-in). **The right retrofit is a
small additive enhancement to the existing Caveman rules block** — Option A folded
into the existing system, not a port, not a new surface.

## Impact Assessment

### Affected Components

- `sdk/examples/readme-example-2.ts` (Finding 1)
- `agents/basher.ts` system prompt (Finding 2)
- `cli/src/commands/export-conversation.ts` + `cli/src/commands/graph-export.ts` +
  `cli/src/commands/graph-export/template.ts` (Finding 3)
- `packages/agent-runtime/src/tools/tool-executor/native.ts` + `transition-phase.ts` (Finding 4)
- `packages/agent-runtime/src/util/caveman-rules.ts` + tests (Finding 5)

### Risk Level

- [x] High: one hard-gate failure (ESLint) blocks release; one harness deadlock burns
  tokens in every self_correct path; export UX degrades at scale
- [ ] Critical
- [ ] Medium
- [ ] Low

## Proposed Solution

### Approach

Five surgical, independent fixes — no shared architecture, no new services. Each is
small (< 30 lines) and validated by the existing gates.

### Steps

1. **Finding 1 (ESLint):** Move the `/* eslint-disable no-console */` comment in
   `sdk/examples/readme-example-2.ts` below all imports (after line 6), so the two
   `@savant-code/sdk` imports are adjacent. Re-run `bun x eslint . --max-warnings 0` →
   exit 0.
2. **Finding 2 (basher fail-fast):** Add a fail-fast instruction to the basher
   `systemPrompt`: if the first tool attempt reports the FSM gate block, reply with a
   single line naming the block and exit — never analyze/explain/offer alternatives.
   (Parent-side guidance already exists at `system-prompt.ts:143`; this closes the
   subagent side.)
3. **Finding 3 (exports):**
   - Default output paths → `dev/exports/conversation/savant-export.html` (rotate:
     overwrite previous) and `dev/exports/graph/savant-graph.html` (rotate), with the
     user-supplied path honored when given. Keep `.gitignore` patterns
     (`savant-export-*.html` / `savant-graph-*.html`) working.
   - Graph template: defer Cytoscape init until DOM ready; lazy-parse `GRAPH_DATA`;
     zoom-to-fit + cluster-based spread on load; make `.graph-sidebar`
     `position: fixed` within the viewport with responsive fallback.
4. **Finding 4 (self_correct terminal):** Extend the `run_terminal_command` FSM allowlist
   at `native.ts:170` from `['audit', 'green']` to `['audit', 'green', 'self_correct']`,
   matching the documented phase table (`common/src/constants/agents.ts:202`).
   Add/extend the phase-gating test in `tool-validation-error.test.ts`.
5. **Finding 5 (ADHD retrofit):** Extend `buildCavemanRulesBlock` in
   `packages/agent-runtime/src/util/caveman-rules.ts` with three additive rules when
   enabled: *number multi-step tasks; cap lists at 5; end with one concrete next step*.
   No new mode, no new skill, no config surface change. Update the module doc comment to
   note the ADHD-derived enhancement (idea perfected into our system, not a port). Answer
   Nova's question with this decision.

### Verification

- `bun x eslint . --max-warnings 0` → exit 0 (F1)
- `cd packages/agent-runtime && bun test src/__tests__/tool-validation-error.test.ts
  src/util/__tests__/caveman-rules.test.ts` → green (F4/F5)
- `cd cli && bun test src/commands/__tests__/export-conversation.test.ts
  src/commands/__tests__/graph-export.test.ts` → green (F3)
- Typecheck ×4 (`sdk`, `common`, `packages/agent-runtime`, `cli`) → exit 0
- `bun run lint:md` → exit 0; `bunx prettier --check .` → clean
- Live `/graph-export` + `/export` smoke to `dev/exports/` in the e2e harness (`dev/test-prompts/graph-export-e2e.ts`)

## Perfection Loop

### Loop 1

- **RED:** Five findings cataloged with file:line evidence (above). ADHD evaluated honestly
  per operator guidance: 3 of 10 rules add value, 1 dropped, 6 already covered by Caveman.
- **GREEN:** Five minimal fixes proposed (above). Missed questions answered below. Most robust defaults chosen.
- **AUDIT (Verifier, code-reviewer-glm):** All 8 factual claims **PASS** with file:line
  citations — (1) `sdk/examples/readme-example-2.ts:3-6` comment splits import group;
  (2) `native.ts:170` allowlist `['audit','green']`; (3) `common/src/constants/agents.ts:202`
  grants run_terminal_command to self_correct (mismatch real); (4) `transition-phase.ts:29-37`
  self_correct transitions + `:83-97` FID gate; (5) `export-conversation.ts:60` +
  `graph-export.ts:66` write to `process.cwd()`; (6) `graph-export/template.ts:130` inline
  GRAPH_DATA, `:531-535` sidebar as flex child, `:209-211` COSE; (7) `caveman-rules.ts` +
  `strings.ts:290` wiring; (8) `basher.ts` systemPrompt has no fail-fast. Verifier raised
  6 gaps (below).
- **ADVERSARIAL (meta-verification):** 6/6 Verifier findings CONFIRMED/ADJUSTED, 0 REFUTED:
  1. CONFIRMED — `.gitignore` rotation gap: `dev/exports/conversation/savant-export.html`
     (no dash) would NOT match `savant-export-*.html`; add `dev/exports/` to `.gitignore`.
  2. CONFIRMED — `common/src/constants/agents.ts:205` note says `run_terminal_command
     (green/audit only)` while L202 grants self_correct; reconcile with the runtime fix.
  3. CONFIRMED — `native.ts:174` block message + `tool-validation-error.test.ts:992` assert
     old allowlist; update message and add a positive self_correct test.
  4. ADJUSTED (evidence) — `dev/session-summaries/2026-07-25-2000.md:67` records the known
     `run_readonly_command` workaround; allowlist fix still correct (run_readonly_command
     cannot run mutating verification).
  5. CONFIRMED risk — `graph-export.test.ts` / `export-conversation.test.ts` may assert old
     default CWD paths; check/update during implementation (e2e harness passes explicit
     fixture paths, safe).
  6. ADJUSTED wording — "lazy" means deferring Cytoscape construction to after first paint /
     `requestIdleCallback`; the JSON stays inline (single-file constraint).
- **CHANGE DELTA:** ~10 files touched, ~150 lines net

### Nova ELK feature request (2026-08-06-graph-export-elk-layout-feature-request.md) — loop verdict

**Finding:** Nova requests switching the graph export from COSE to the ELK layout engine (elkjs in a Web
Worker, or server-side precompute) because COSE produces a hairball at ~7k nodes.

**Verdict: DEFERRED — retrofit the idea, don't port the engine** (operator guidance: "perfect the idea into our
system, not a 1:1 port; if we don't really need it, say that, if we can retrofit it, say that").

| ELK request item | Verdict | Rationale |
|---|---|---|
| Layered, non-overlapping layout at scale | **Adopted (retrofitted)** | Finding 3 ships cluster-seeded starting positions + `randomize: false` + boosted repulsion/component-spacing — the hierarchical-layout idea applied to our existing Cytoscape COSE renderer, zero new deps |
| elkjs WASM in the export | **Rejected now** | `elkjs` is not vendored; embedding WASM (or a Web Worker) into a self-contained single-file `file://` HTML is hostile territory (worker/blob constraints, 3.8–4.4 MB files grow further) |
| Server-side ELK precompute (Option C) | **Deferred with trigger** | Only ELK variant that fits the single-file constraint, but adds a Bun-side WASM dep + export-time layout cost for a payoff not yet proven needed |
| React Flow + ELK rewrite (Option B) | **Rejected** | Full template rewrite; contradicts FID-2026-0806-006's stack decision (keep Cytoscape, offline, already bundled) |

**Revisit trigger:** if the cluster-seeded COSE still overlaps on the real 6,916-node export after this fix,
reopen and evaluate Option C (server-side ELK positions embedded as a Cytoscape `preset` layout).

### Missed Questions

1. **Should exports default to `dev/exports/` or keep CWD?** → Default to
   `dev/exports/{conversation,graph}/` with rotation; explicit user paths still honored. The
   Nova request asked for it; `.gitignore` already covers the filename patterns, and the
   session summaries reference exports by name — rotation keeps one current file per kind,
   archive is out of scope (YAGNI) unless requested.
2. **Should the basher self-check live in the prompt or the runtime?** → Prompt. The runtime
   block message is already clear; the waste is the *model's* ceremony. A one-line
   instruction in `basher.ts` systemPrompt is the smallest surface. A runtime short-circuit
   (detecting idle-phase spawn and short-returning) would hide intent and skip the parent's
   transition responsibility.
3. **Does `self_correct` really need terminal access, or should the fix be a new transition
   `self_correct → audit`?** → Terminal access. ECHO.md and the phase table already grant
   `run_terminal_command` to self_correct for *inline* verification ("SELF_CORRECT →
   complete: … verify inline"). Adding `self_correct → audit` would also work but changes
   the FSM surface and adds a transition test churn; the documented inline path is the most
   robust default.
4. **Caveman/ADHD: does the user want a visible toggle?** → No. The existing
   `caveman.enabled` config flag is the toggle; the ADHD rules ride the same flag.
   Surfacing a second toggle before the first is used would be speculative (YAGNI).
5. **Should the ESLint fix be a disable-comment move or a restructure?** → Move the comment.
   The file is an SDK example; minimal diff, no behavior change, satisfies import/order
   without eslint-disable-file noise.

### Code Verification Evidence

- [x] Files referenced in "Affected Components" exist in the codebase
- [x] Implementation matches the proposed solution
- [x] Typecheck passes: SDK 0 / common 0 / agent-runtime 0 / CLI 0
- [x] `bun x eslint . --max-warnings 0` → exit 0 (F1 gate cleared)
- [x] `bun run lint:md` → exit 0
- [x] `bunx prettier --check .` → clean
- [x] agent-runtime tests (tool-validation-error + caveman-rules) → 38 pass / 0 fail
- [x] CLI export/graph tests → 15 pass / 0 fail
- [x] FID status updated to reflect actual implementation state (below)

### Loop 2 (if needed)

- **RED:** *(filled after loop 1 audit)*
- **GREEN:** *(filled if findings)*
- **AUDIT:** *(filled if findings)*
- **CHANGE DELTA:** *(filled if findings)*

## Resolution

- **Fixed By:** Savant (Orchestrator) — FID-2026-0806-016 loop, session 2026-08-06
- **Fixed Date:** 2026-08-06
- **Fix Description:**
  - **F1 (ESLint gate):** `sdk/examples/readme-example-2.ts` — the `eslint-disable no-console`
    comment moved below the import block; the two `@savant-code/sdk` imports are adjacent
    (blank line added by `eslint --fix` per `import/order newlines-between: always`).
    `bun x eslint . --max-warnings 0` now exits 0.
  - **F2 (basher fail-fast):** `agents/basher.ts` system prompt gains a fail-fast clause — on
    an FSM-gate block reply ONE line (`BLOCKED: …`) and stop; no analysis/explanations/
    alternatives. Bundled agents regenerated (`cli/src/agents/bundled-agents.generated.ts`
    contains the clause).
  - **F3 (exports):** `/export` + `/graph-export` default paths moved to
    `dev/exports/conversation/savant-export.html` + `dev/exports/graph/savant-graph.html`
    (single-file rotation, user-supplied paths honored); `.gitignore` gains `dev/exports/`;
    graph template defers Cytoscape init until DOM ready, lazy-constructs the graph,
    cluster-seeds node spread, zoom-to-fit on load, and makes the sidebar viewport-fixed.
  - **F4 (self_correct deadlock):** `native.ts` `run_terminal_command` FSM allowlist extended
    `['audit','green']` → `['audit','green','self_correct']`; block message updated;
    `common/src/constants/agents.ts` note reconciled (self_correct row + note both grant
    the tool); positive self_correct test added to `tool-validation-error.test.ts`.
  - **F5 (ADHD retrofit):** `caveman-rules.ts` `buildCavemanRulesBlock` gains three additive
    ADHD-derived rules under the existing `caveman.enabled` flag: number multi-step tasks,
    cap lists at 5, end with one concrete next step. No new mode/skill/config. Nova feature
    question answered (see below).
  - **ELK request:** DEFERRED with trigger — cluster-seeded COSE is the retrofit; Option C
    (server-side ELK preset) re-evaluated if the real 6,916-node export still overlaps.
- **Tests Added:**
  - `packages/agent-runtime/src/__tests__/tool-validation-error.test.ts` — positive
    self_correct `run_terminal_command` test (F4)
  - `packages/agent-runtime/src/util/__tests__/caveman-rules.test.ts` — assert the three
    ADHD-derived structure rules (F5)
- **Verified By:** Savant (Orchestrator) — validation run 2026-08-06: typecheck ×4 exit 0,
  ESLint exit 0, lint:md exit 0, prettier clean, 38 + 15 targeted tests pass / 0 fail
- **Commit/PR:** *(not pushed — working tree)*
- **Archived:** 2026-08-06 → `dev/fids/archive/` (Auto-Archive rule)

## Lessons Learned

- An audit run exercising the harness is itself the best integration test for the harness —
  the self_correct deadlock was invisible until a read-only audit tried to write its report
  and verify it.
- "Port the idea" is the wrong frame for features like ADHD mode; the right frame is "which
  rules does our system already satisfy, which add value, which don't fit" — then retrofit
  only the value-adds onto the existing surface.
- Zero-tolerance lint gates catch exactly what they should: a one-line comment placement that a human review would miss.
