/**
 * Branding assets shared by the export template and message rows.
 */
import { CHARACTER_LOGO_DATA_URI } from '../graph-export/character'

// Logo data URI shared by the header and Savant row markers. Uses the same
// character logo as the graph export (FID-2026-0807-009 branding alignment)
// so both export surfaces are consistent.
export const LOGO_DATA_URI = CHARACTER_LOGO_DATA_URI
